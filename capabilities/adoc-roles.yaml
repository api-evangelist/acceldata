naftiko: 1.0.0-alpha2
info:
  label: Acceldata - Data Observability Cloud API — Roles
  description: 'Acceldata - Data Observability Cloud API — Roles. 1 operations. Lead operation: Acceldata List Roles. Self-contained
    Naftiko capability covering one Acceldata business surface.'
  tags:
  - Acceldata
  - Roles
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ACCELDATA_API_KEY: ACCELDATA_API_KEY
capability:
  consumes:
  - type: http
    namespace: adoc-roles
    baseUri: https://api.acceldata.app/v1
    description: Acceldata - Data Observability Cloud API — Roles business capability. Self-contained, no shared references.
    resources:
    - name: roles
      path: /roles
      operations:
      - name: listroles
        method: GET
        description: Acceldata List Roles
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ACCELDATA_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: adoc-roles-rest
    port: 8080
    description: REST adapter for Acceldata - Data Observability Cloud API — Roles. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/roles
      name: roles
      description: REST surface for roles.
      operations:
      - method: GET
        name: listroles
        description: Acceldata List Roles
        call: adoc-roles.listroles
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: adoc-roles-mcp
    port: 9090
    transport: http
    description: MCP adapter for Acceldata - Data Observability Cloud API — Roles. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: acceldata-list-roles
      description: Acceldata List Roles
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: adoc-roles.listroles
      outputParameters:
      - type: object
        mapping: $.
